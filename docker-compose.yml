version: '3'

services:
  f1tenth-ros:
    build:
      context: .
      dockerfile: Dockerfile
    stdin_open: true
    volumes:
      - ./compose/vnc/setup.sh:/setup.sh
    ports:
      - 5900:5900 
      - 6080:6080
